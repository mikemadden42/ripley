---
- hosts: device
  tasks:
    - name: Install packages
      apt:
        name: "{{ packages }}"
      vars:
        packages:
          - alacritty
          - handbrake
          - keepassxc
          - sound-juicer
