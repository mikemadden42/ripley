---
- hosts: device
  tasks:
    - name: Install packages
      apt:
        name: "{{ packages }}"
      vars:
        packages:
          - cheese
          - gnome-tweaks
          - handbrake
          - keepassxc
          - rhythmbox
          - sound-juicer
